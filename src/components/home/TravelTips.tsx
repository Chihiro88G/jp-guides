import * as React from 'react';

import Title from '../Title';
import SectionWrapper from '../SectionWrapper';

export default function TravelTips() {
  return (
    <SectionWrapper bgColor='pink' height='50vh'>
      <Title>Travel Tips</Title>
    </SectionWrapper>
  )
}