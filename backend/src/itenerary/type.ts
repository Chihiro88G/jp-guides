export type IteneraryRecord = {
  id: number,
  title: string,
  included_meal_id: number,
  content: string,
}

export type IteneraryModel = {
  title: string,
  includedMeals: string,
  content: string,
}